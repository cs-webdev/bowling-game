bowling-game
============

A test-driven approach to the bowling game problem. It can be used to exemplify how using TDD leads to simpler yet robust solutions.

`game2.rb` is a possible refactor of `game.rb` done after all tests were passing.
